{
  "name": "gsum-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for gsum project summarization",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/",
    "test:watch": "node --test --watch test/**/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}